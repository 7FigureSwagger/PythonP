#Tuples
#Tuples are immutable lists, cast using () instead of [] 
#Used for when you do not want the data modified

coordinates = (4, 5)
print(coordinates[0])

#can have lists of Tuples
places = [(4, 5), (9, 10), (7, 8)]
print(places)